/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
};
module.exports = {
  output: 'standalone', // Recommended for DO
}

export default nextConfig;
